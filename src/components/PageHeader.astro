---
type Props = {
  eyebrow: string;
  title: string;
  intro: string;
};

const { eyebrow, title, intro } = Astro.props;
---

<header class="page-header">
  <p class="eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  <p>{intro}</p>
</header>
